---
- hosts: apache_servers
  become: true      #have root access
  gather_facts: yes
  name: setup apache web server
  roles:
  # install and configure apache service
    - { role: apache, become: True, tags: setup_apache }
